<div class="container">
  <div class="row">
     <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">

        <h1>Editing Book</h1>
        
         <% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
        
        <%= form_with model: @book do |f| %>

          <div class="form-group">
            <label for="book_title">Title</label>
          <%= f.text_field :title, autofocus: true, id:"inputName", placeholder:""%><br>

          <div class="form-group">
          <label for="inputImage">Opinion</label>
          <%= f.text_field :body, autofocus: true, id:"inputName", placeholder:"名前"%>
          </div>
          <div class="form-group">
          <input type="submit" name="commit" value="Update Book" class="btn btn-success" data-disable-with="Update Book"><br>
          </div>
          <span>
          <%= link_to "Show", book_path(@book.id) %>
          </span>
          |
        　<a class="back" href="/books">Back</a>
        <% end %>
        </div>
  　　　</div>
    </div>
  </div>
